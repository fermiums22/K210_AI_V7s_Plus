#pragma once

/* Template for Wi-Fi credentials.
 * Copy this file to src/wifi_cfg.h and fill in real values — wifi_cfg.h is
 * gitignored so the secrets never reach the repository. 2.4 GHz only. */
#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"
